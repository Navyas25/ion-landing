import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ION | Classic Alpha — Pure Energy. Zero Limits.",
  description:
    "Experience ION Classic Alpha. Pure energy engineered for those who push further.",
  openGraph: {
    title: "ION | Classic Alpha",
    description: "Pure Energy. Zero Limits.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full bg-[#050508] text-white overflow-x-hidden">
        {children}
      </body>
    </html>
  );
}
